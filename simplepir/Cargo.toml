[package]
name        = "simplepir"
version     = "0.1.0"
edition.workspace      = true
rust-version.workspace = true
authors.workspace      = true
repository.workspace   = true
license.workspace      = true
description = "Row-KOPIR (SimplePIR) single-server index PIR backend — core logic for the KPIR^index re-implementation"

[lints]
workspace = true

[dependencies]
rand        = { workspace = true }
rand_chacha = { workspace = true }

[dev-dependencies]
rand = { workspace = true }
